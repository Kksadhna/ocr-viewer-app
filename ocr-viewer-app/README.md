# OCR Viewer App (Vite + React + Tailwind + Tesseract.js)

This is a ready-to-run sample project that includes your OCRViewer component,
wired to Tesseract.js for browser OCR (images only).

How to run:

```bash
npm install
npm run dev
```

Open http://localhost:5173
